import React from 'react';
import { Text } from 'react-native';
import atomsStyle from '../style/atomsStyle';

const NewsPopUpText = ({ text }) => <Text style={atomsStyle.newsPopUpText}>{text}</Text>;

export default NewsPopUpText;
